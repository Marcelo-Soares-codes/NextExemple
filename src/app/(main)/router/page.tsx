import { Heading } from '../../../components/Heading/index';

export default function Router() {
  return (
    <body>
      <Heading>Assim que se cria uma rota chamada /router</Heading>
    </body>
  );
}
